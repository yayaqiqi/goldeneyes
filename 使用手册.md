# 恋综机器人使用手册

所有命令均在 KOOK 频道内以文字消息发送，严格区分大小写和符号。

***

## 一、管理员命令

### 创建恋综

```
创建·恋综名称
```

在当前服务器下创建一个新的恋综，名称不能为空或超过 10 个字。

### 清空恋综

```
清空·恋综名称
```

删除该恋综的所有数据，不可恢复。

### 绑定个人频道

```
绑定·恋综名称 角色名称 @玩家账号 @角色组
```

将当前频道绑定到指定角色。绑定后频道名即为角色代号。每个频道只能绑定一个角色。

### 移除绑定

```
移除绑定·恋综名称
```

解除当前频道的角色绑定。

### 更改恋综配置

```
更改信息·恋综名称 属性 key value
```

修改恋综 JSON 配置中的字段。常用场景如设置分组 ID（parents）、公共频道 ID（public\_channels）等。

### 修正已删除频道

```
修正删除频道·恋综名称
```

清理玩家记录中已失效（被删除）的频道数据。

### 进入EP

```
进入EP：EP编号
```

设置当前恋综的当前 EP 编号（如 1、2、3），用于心愿墙发布等需要匹配 EP 的功能。

### 检查超时对戏

```
检查超时·恋综名称
```

结束所有超过 72 小时未回复的对戏。

### 提醒回戏

```
提醒回戏·恋综名称
```

对超过 24 小时未回复的玩家发送 @提醒。

### 创建频道

```
创建频道·EP1
官约：角色A&角色B
踩点：角色C&角色D
```

在对应 EP 的频道分类下批量创建对戏频道。频道名称格式：`官约：角色A&角色B`。

***

## 二、玩家互动命令

### 创建通讯频道

```
通讯·角色A&角色B
```

为两个或多个角色创建私密通讯频道。自己的名字必须放在第一个。

### 发起私约

```
私约·EP名称
发起人：角色名
接收人：角色名
时间：
地点：
留言：
```

向对方发送带「接受/拒绝」按钮的私约邀请卡片。接受后自动创建私约频道。发起人必须与当前频道绑定的角色一致。

> **按钮操作**：接收人在卡片上点击「接受」或「拒绝」，无需手动输入命令。

### 传纸条

```
To角色名 纸条内容
```

以卡片消息的形式向目标角色的个人频道发送匿名纸条。`To` 与角色名之间无空格，角色名后空一格接内容。

### 骰子

| 指令 | 说明 | 示例结果 |
|------|------|----------|
| `.rd80` | 投掷 1 颗 80 面骰 | `🎲 64 结果：**64**` |
| `.r3d6` | 投掷 3 颗 6 面骰 | `🎲 {3+1+6}(10) 结果：**10**` |
| `.r2d6+3` | 投掷 2 颗 6 面骰并加 3 | `🎲 {4+3}(7)+3 结果：**10**` |
| `.ra80` | 态度骰，目标值 80 | `🎲 1d100 目标80 投掷：**69** ✅ 成功` |
| `.ra智力80` | 带属性名的态度骰 | `🎲 1d100 智力：目标80 投掷：**23** ✅ 困难成功` |
| `.ra` | 态度骰，默认目标值 50 | `🎲 1d100 目标50 投掷：**4** 🌟 极难成功` |

**态度骰判定规则**：

| 结果 | 判定 |
|------|------|
| 1 | 💥 大失败 |
| 100 | ✨ 大成功 |
| ≤目标÷5 | 🌟 极难成功 |
| ≤目标÷2 | ✅ 困难成功 |
| ≤目标 | ✅ 成功 |
| ≥96 | 💀 大失败 |
| >目标+(差值÷5) | ❌ 困难失败 |
| >目标 | ⚠️ 失败 |

### 送礼物

```
礼物·送礼对象
送礼者：可匿名
收礼者：可代号
时间：
内容：
留言：
```

支持附带图片（发送卡片消息时附加图片即可）。

### 点歌

```
点歌台
点歌人：
收听者：
时间：
留言：
歌曲：
```

点歌内容发送到公共点歌频道，歌曲链接单独发送。

### 公共留言

```
公共留言
FROM：
时间：
内容：
```

发布一条公共留言到留言频道。

### 发布心愿

```
心愿·EP名称
署名：
内容：
时间：
```

在心愿墙发布心愿，其他玩家可点击「摘取」按钮领取。EP 必须与当前设置的 `current_EP` 一致。摘取后自动创建心愿频道。

### 踩点

```
踩点·A、B、C、D、E、F
```

发送踩点卡片，显示可踩点的地点按钮。玩家点击按钮选择踩点地点

> **按钮操作**：玩家在卡片上点击相应地点按钮即可选择踩点，玩家可多次点击更改，但只记录玩家最后一次点击内容。

### 查看踩点信息

```
查看踩点信息
```

查看当前恋综的踩点统计，按地点展示每个地点有哪些玩家选择。

### 心动信

```
心动信
发信人：
收信人：
内容：
```

提交一封心动信，由管理员统一发送。支持附带图片。收信人必须是已绑定的角色名。

### 撤回心动信

```
撤回心动信
```

撤回当前频道已提交的所有心动信。

***

## 三、档案与记录命令

### 查看个人档案

```
个人档案
```

在个人频道内发送，查看当前角色的对戏进度、字数统计和回戏状态。

> 状态图标说明：🟢 24h 内 ｜ 🟡 24–48h ｜ 🔴 超过 48h ｜ ✅ 已结戏

### 结束对戏

```
结束对戏
```

在对戏频道内发送，标记当前对戏为已完成，更新所有人的记录。

### 更新记录

```
更新记录
```

手动刷新当前频道的对戏记录（从频道消息重新读取）。

### 手动添加记录

```
添加记录·EP名称
```

在对戏频道内发送，为该频道手动创建一条对戏记录。

### 生成戏录

```
生成戏录·恋综名称 角色名称
```

生成该角色的 HTML 戏录文件，包含总场次、总字数、目录导航和完整对戏内容。

***

## 四、自动任务

每天 12:00（上海时间）机器人自动执行：

- 清理已删除频道的残留记录
- 结束超过 72 小时的超时对戏
- 提醒超过 24 小时未回戏的玩家

***

## 五、对戏规则

在对戏频道中，每条消息的第一行必须是**当前发言角色的代号词**（即绑定的角色名），机器人据此自动追踪对戏轮次和字数。示例：

```
小明
今天天气不错，我们去散步吧。

小红
好啊，去哪里呢？
```

